<style>
    .table-container {
        border: 1px solid #ddd; /* Border for the entire table */
    }

    th {
        background-color: lightblue; /* Background color for the header cells */
    }

    tr:nth-child(even) {
        background-color: lightgrey; /* Background color for every second row */
    }

    td, th {
        border: 1px solid #ddd; /* Border for each cell */
        padding: 8px; /* Adjust the padding as needed */
        text-align: left;
    }
    .editable {
        border: 1px solid #ddd;
        padding: 8px;
    }

    .flex-container {
        display: flex;
        justify-content: flex-end; /* Align to the right */
        margin-bottom: 10px;
    }

    .btn {
        margin-left: 10px; /* Add margin between buttons */
        display: inline-block;
        padding: 10px 20px;
        background-color: #4CAF50; /* Green background color */
        color: white; /* White text color */
        text-align: center;
        text-decoration: none;
        font-size: 16px;
        cursor: pointer;
        border: none;
        border-radius: 4px;
    }

    .btn:hover {
        background-color: #45a049; /* Darker green on hover */
    }
</style>

<%- include('../components/sidebar') %>



<section class="orders lg:ml-72  light-section ">
    <div class="w-11/12 xl:w-5/6 mx-auto pt-12 pb-2 ">
        <h1 class="font-bold text-3xl mb-4">All Orders</h1>
        <div class="search-container mb-4 ">
           
            <form action="/admin/orders/search" method="GET" class="flex">
                <input
                    type="text"
                    id="search-input"
                    name="name"
                    class="search-input w-64 sm:w-72"
                    placeholder="Search orders"
                />
                <button type="submit" class="search-button">
                    <span>&#128269;</span>
                </button>
            </form>            
        </div>
        <div class="overflow-auto">

            <table class="w-full table-auto bg-white">
                <thead>
                    <tr>
                        <th class="px-4 py-2 text-left">ID</th>
                        <th class="px-4 py-2 text-left">Email</th>
                        <th class="px-4 py-2 text-left">Preis</th>
                        <th class="px-4 py-2 text-left">status</th>
                    <th class="px-4 py-2 text-left">Placed at</th>
                </tr>
            </thead>
            <tbody id="orderTableBody">
            </tbody>
        </table>
    </div>
    </div>
</section>

<style>
    /* .orders {
        margin-left: 280px;
    } */
</style>
