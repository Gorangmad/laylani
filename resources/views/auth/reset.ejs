<section class="login flex justify-center pt-24">
    <div class="w-full max-w-xs">
        <form action="/neuesPasswort" method="POST" class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
          <div class="mb-4">
            <% if(messages.error) {%>
              <span class="text-red-500 text-small"><%= messages.error%></span>
              <% } %>
            <label class="block text-gray-700 text-sm font-bold mb-2" for="password1">
              Passwort
            </label>
            <input name="Passwort"
             class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" id="password" type="text" placeholder="Passwort">
             <label class="block text-gray-700 text-sm font-bold mb-2" for="password2">
              Passwort wiederholen
            </label>
            <input name="Passwort"
             class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" id="password" type="text" placeholder="Passwort">
          </div>
          <button class="btn-primary rounded-full text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="submit">
            Passwort zurücksetzen
          </button>
        </form>
      </div>
</section>